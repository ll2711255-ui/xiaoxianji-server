@import '../../../utils/printer-panel.wxss';

.mpro-page {
  min-height: 100vh;
  background: #F5F5F5;
  display: flex;
  flex-direction: column;
}

.mpro-scroll {
  flex: 1;
  height: 0;
}

/* Header */
.mpro-header {
  display: flex;
  align-items: center;
  background: linear-gradient(135deg, var(--color-brand-dark-red), var(--color-brand-main-red));
  padding: 32rpx 32rpx 40rpx;
  gap: 20rpx;
}
.mpro-avatar {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.12);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48rpx;
}
.mpro-shop-info {
  flex: 1;
}
.mpro-shop-name {
  font-size: 38rpx;
  font-weight: 700;
  color: #fff;
  display: block;
}
.mpro-shop-id {
  font-size: 26rpx;
  color: rgba(255,255,255,0.4);
  margin-top: 4rpx;
  display: block;
}
.mpro-role-tag {
  background: rgba(255,255,255,0.15);
  color: #fff;
  font-size: 26rpx;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
}

/* Section */
.mpro-sec-title {
  font-size: 28rpx;
  color: #999;
  display: block;
  margin-bottom: 8rpx;
}

/* Menu item */
.mpro-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx 0;
}
.mpro-item + .mpro-item {
  border-top: 1rpx solid #F5F5F5;
}
.mpro-item:active {
  opacity: 0.6;
}
.mpro-item-left {
  display: flex;
  align-items: center;
  gap: 16rpx;
  font-size: 34rpx;
  color: #1A1A1A;
}
.mpro-item-icon {
  font-size: 40rpx;
}
.mpro-arrow {
  font-size: 40rpx;
  color: #C7C7CC;
}

/* Logout */
.mpro-logout-wrap {
  padding: 48rpx 24rpx;
  text-align: center;
}
.mpro-logout-btn {
  width: 100%;
  background: #fff;
  color: #FF3B30;
  border: 1rpx solid #FF3B30;
  border-radius: 40rpx;
  font-size: 32rpx;
  padding: 20rpx 0;
}
.mpro-version {
  display: block;
  font-size: 26rpx;
  color: #C7C7CC;
  margin-top: 24rpx;
}

/* ====================================================================
   配送设置弹窗
   ==================================================================== */
.mpro-modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1001;
  display: flex;
  align-items: center;
  justify-content: center;
}
.mpro-modal-card {
  width: 600rpx;
  background: #fff;
  border-radius: 24rpx;
  overflow: hidden;
}
.mpro-modal-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx 32rpx 16rpx;
}
.mpro-modal-title {
  font-size: 38rpx;
  font-weight: 700;
  color: #1A1A1A;
}
.mpro-modal-close {
  font-size: 48rpx;
  color: #C7C7CC;
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}
.mpro-modal-body {
  padding: 0 32rpx 24rpx;
}
.mpro-modal-desc {
  font-size: 30rpx;
  color: #999;
  line-height: 1.6;
  display: block;
  margin-bottom: 28rpx;
}
.mpro-modal-field {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #F8F8F8;
  border-radius: 12rpx;
  padding: 20rpx 24rpx;
}
.mpro-modal-label {
  font-size: 32rpx;
  color: #333;
  flex-shrink: 0;
}
.mpro-modal-input-row {
  display: flex;
  align-items: center;
  gap: 8rpx;
}
.mpro-modal-input {
  width: 120rpx;
  height: 64rpx;
  background: #fff;
  border: 1rpx solid #E5E5E5;
  border-radius: 8rpx;
  text-align: center;
  font-size: 36rpx;
  font-weight: 600;
  color: #1A1A1A;
}
.mpro-modal-unit {
  font-size: 30rpx;
  color: #666;
}
.mpro-modal-hint {
  font-size: 26rpx;
  color: #C7C7CC;
  margin-top: 12rpx;
  display: block;
}
.mpro-modal-foot {
  display: flex;
  border-top: 1rpx solid #F0F0F0;
}
.mpro-modal-cancel,
.mpro-modal-save {
  flex: 1;
  height: 96rpx;
  line-height: 96rpx;
  text-align: center;
  font-size: 34rpx;
  background: #fff;
  border: none;
  border-radius: 0;
  padding: 0;
}
.mpro-modal-cancel {
  color: #999;
  border-right: 1rpx solid #F0F0F0;
}
.mpro-modal-save {
  color: #1DB96A;
  font-weight: 600;
}
.mpro-modal-cancel::after,
.mpro-modal-save::after {
  display: none;
}

/* 店铺信息弹窗 — 整行输入框 */
.mpro-modal-field-block {
  margin-bottom: 28rpx;
}
.mpro-modal-input-full {
  width: 100%;
  height: 80rpx;
  background: #F8F8F8;
  border-radius: 12rpx;
  padding: 0 24rpx;
  font-size: 32rpx;
  color: #1A1A1A;
  box-sizing: border-box;
  margin-top: 12rpx;
}

/* 地址选择行 */
.mpro-addr-row {
  display: flex;
  align-items: center;
  background: #F8F8F8;
  border-radius: 12rpx;
  padding: 16rpx 24rpx;
  margin-top: 12rpx;
}
.mpro-addr-text {
  flex: 1;
  font-size: 32rpx;
  color: #1A1A1A;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.mpro-addr-placeholder {
  color: #C7C7CC;
}
.mpro-addr-pin {
  flex-shrink: 0;
  font-size: 30rpx;
  color: #1DB96A;
  font-weight: 600;
  margin-left: 16rpx;
  padding: 8rpx 0;
}
.mpro-addr-pin:active {
  opacity: 0.6;
}

/* 地图预览 */
.mpro-map-wrap {
  margin-top: 20rpx;
  width: 100%;
  height: 320rpx;
  border-radius: 12rpx;
  overflow: hidden;
  border: 1rpx solid #E5E5E5;
}
.mpro-map {
  width: 100%;
  height: 100%;
}
