@import '../../../utils/printer-panel.wxss';

.mo-page {
  min-height: 100vh;
  background: var(--color-bg-page);
  display: flex;
  flex-direction: column;
}

/* ========== 顶部段切换 ========== */
.mo-segment {
  display: flex;
  background: linear-gradient(135deg, var(--color-brand-dark-red), var(--color-brand-main-red));
  padding: 20rpx 48rpx 16rpx;
  gap: 0;
}
.mo-seg-item {
  flex: 1;
  text-align: center;
  padding: 18rpx 0;
  font-size: var(--font-lg);
  font-weight: 600;
  color: rgba(255,255,255,0.7);
  border-bottom: 4rpx solid transparent;
  transition: all 0.2s;
}
.mo-seg-item.on {
  color: #fff;
  border-bottom-color: #FFFFFF;
}

/* ========== 线上统计 ========== */
.mo-stats-bar {
  display: flex;
  background: linear-gradient(180deg, var(--color-brand-main-red), var(--color-brand-chili-red));
  padding: 0 32rpx 28rpx;
  gap: 0;
}
.mo-stat {
  flex: 1;
  text-align: center;
}
.mo-stat-num {
  font-size: 48rpx;
  font-weight: 800;
  color: #fff;
  display: block;
}
.mo-stat-label {
  font-size: var(--font-sm);
  color: rgba(255,255,255,0.8);
  margin-top: 4rpx;
  display: block;
}

/* 子 Tab */
.mo-tabs {
  display: flex;
  background: var(--color-brand-chili-red);
  padding: 0 24rpx 16rpx;
  gap: 8rpx;
}
.mo-tab {
  flex: 1;
  text-align: center;
  padding: 14rpx 0;
  font-size: var(--font-sm);
  color: rgba(255,255,255,0.85);
  background: rgba(255,255,255,0.18);
  border-radius: var(--radius-md);
  white-space: nowrap;
}
.mo-tab.on {
  color: var(--color-primary);
  font-weight: 700;
  background: var(--color-bg-card);
}

/* 配送/待取货 子 Tab */
.mo-sub-tabs {
  display: flex;
  background: var(--color-brand-chili-red);
  padding: 0 48rpx 20rpx;
  gap: 24rpx;
}
.mo-sub-tab {
  flex: 1;
  text-align: center;
  padding: 16rpx 0 10rpx;
  font-size: var(--font-base);
  font-weight: 600;
  color: rgba(255,255,255,0.7);
  border-bottom: 4rpx solid transparent;
  transition: all 0.2s;
}
.mo-sub-tab.on {
  color: #fff;
  border-bottom-color: #FFFFFF;
}

/* ========== 滚动区 ========== */
.mo-scroll {
  flex: 1;
  height: 0;
}

.mo-body {
  padding: 16rpx 24rpx;
}

/* ========== 订单卡片 ========== */
.mo-card {
  background: var(--color-bg-card);
  border-radius: var(--radius-md);
  padding: 20rpx;
  margin-bottom: 16rpx;
  box-shadow: 0 2rpx 6rpx rgba(0,0,0,0.06);
}
.mo-card-new {
  border-left: 6rpx solid var(--color-primary);
  padding-left: 16rpx;
}

.mo-head {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 14rpx;
}
.mo-head-left {
  display: flex;
  align-items: center;
  gap: 12rpx;
}
.mo-no {
  font-size: var(--font-base);
  font-weight: 700;
  color: var(--color-text-1);
}
.mo-type-tag {
  font-size: var(--font-sm);
  padding: 2rpx 10rpx;
  border-radius: var(--radius-sm);
  background: var(--color-bg-page);
  color: var(--color-text-3);
}
.mo-time {
  font-size: var(--font-sm);
  color: var(--color-text-3);
  margin-left: 4rpx;
}
.mo-tag-offline {
  background: var(--color-warning-bg);
  color: var(--color-warning);
}
.mo-tag-wechat {
  background: var(--color-primary-pale);
  color: var(--color-primary);
}
.mo-status {
  font-size: var(--font-md);
  font-weight: 600;
}
.mo-status-warn { color: var(--color-warning); }
.mo-status-ok { color: var(--color-primary); }
.mo-status-cancel { color: var(--color-danger); }

/* Items */
.mo-items {
  padding: 14rpx 0;
  border-top: 1rpx solid var(--color-border);
  border-bottom: 1rpx solid var(--color-border);
}
.mo-item {
  display: flex;
  align-items: center;
  padding: 6rpx 0;
  gap: 10rpx;
}
.mo-pname {
  font-size: var(--font-base);
  font-weight: 500;
  color: var(--color-text-1);
}
.mo-pspec {
  font-size: var(--font-sm);
  color: var(--color-text-3);
}
.mo-pqty {
  margin-left: auto;
  font-size: var(--font-sm);
  color: var(--color-text-3);
}

/* 配送距离 */
.mo-distance {
  display: flex;
  align-items: center;
  gap: 6rpx;
  padding: 8rpx 0;
}
.mo-distance-text {
  font-size: var(--font-sm);
  color: var(--color-primary);
}

/* Foot */
.mo-foot {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 14rpx;
}
.mo-amount {
  font-size: var(--font-base);
  font-weight: 700;
  color: var(--color-primary);
}
.mo-refund-info {
  padding: 10rpx 0;
  text-align: right;
}
.mo-refund-amount {
  font-size: var(--font-md);
  color: var(--color-danger);
  font-weight: 600;
}
.mo-actions {
  display: flex;
  gap: 10rpx;
  flex-wrap: wrap;
  justify-content: flex-end;
}
.mo-btn {
  font-size: var(--font-sm);
  padding: 8rpx 20rpx;
  border-radius: var(--radius-lg);
  border: none;
  line-height: 1.4;
}
.mo-btn-accept  { background: linear-gradient(to right, var(--color-brand-main-red), var(--color-brand-chili-red)); color: #fff; box-shadow: 0 4rpx 12rpx var(--color-brand-soy-red-shadow); }
.mo-btn-reject  { background: #fff; color: var(--color-danger); border: 2rpx solid var(--color-danger); }
.mo-btn-weigh   { background: linear-gradient(135deg, var(--color-brand-gold), var(--color-brand-yolk)); color: #fff; box-shadow: 0 4rpx 12rpx rgba(245,166,35,0.35); }
.mo-btn-process { background: var(--color-brand-tangerine); color: #fff; }
.mo-btn-deliver { background: var(--color-brand-amber); color: #fff; }
.mo-btn-ready   { background: linear-gradient(135deg, var(--color-brand-gold), var(--color-brand-yolk)); color: #fff; box-shadow: 0 4rpx 12rpx rgba(245,166,35,0.35); }
.mo-btn-done    { background: var(--color-text-3); color: #fff; }
.mo-btn-arrive  { background: var(--color-success); color: #fff; }
.mo-btn-pickup  { background: linear-gradient(135deg, var(--color-brand-gold), var(--color-brand-yolk)); color: #fff; box-shadow: 0 4rpx 12rpx rgba(245,166,35,0.35); }
.mo-btn-cancel  { background: #fff; color: var(--color-danger); border: 2rpx solid var(--color-danger); }
.mo-btn-pay     { background: var(--color-success); color: #fff; }
.mo-btn-retry-refund { background: var(--color-danger); color: #fff; }

/* 已完成标签 */
.mo-done-tag {
  font-size: var(--font-sm);
  color: var(--color-primary);
  font-weight: 600;
  padding: 8rpx 20rpx;
  border-radius: var(--radius-lg);
  background: var(--color-primary-pale);
}

/* ========== 线下新增按钮 ========== */
.mo-offline-add {
  position: fixed;
  bottom: 130rpx;
  left: 0;
  right: 0;
  padding: 16rpx 48rpx;
  background: linear-gradient(180deg, rgba(245,245,245,0) 0%, var(--color-bg-page) 40%);
}
.mo-add-btn {
  width: 100%;
  background: linear-gradient(to right, var(--color-brand-main-red), var(--color-brand-chili-red));
  color: #fff;
  font-size: var(--font-lg);
  font-weight: 700;
  border-radius: var(--radius-full);
  border: none;
  padding: 22rpx 0;
  box-shadow: 0 6rpx 20rpx var(--color-brand-soy-red-shadow);
}

/* ========== 空态 ========== */
.empty-state {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 0 80rpx;
  color: var(--color-text-3);
  font-size: var(--font-base);
}
.empty-icon {
  font-size: 68rpx;
  margin-bottom: 16rpx;
}
.empty-sub {
  font-size: var(--font-md);
  color: var(--color-text-3);
  margin-top: 8rpx;
}
.load-more {
  text-align: center;
  padding: 32rpx;
  color: var(--color-text-3);
  font-size: var(--font-md);
}

/* ========== 号码牌网格 ========== */
.md-card-scroll {
  max-height: 420rpx;
  overflow-y: auto;
}
.md-card-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  justify-content: center;
}
.md-card-item {
  width: 100rpx;
  height: 100rpx;
  border-radius: var(--radius-md);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border: 2rpx solid transparent;
}
.md-card-item.idle {
  background: var(--color-success);
  border-color: var(--color-success);
}
.md-card-item.used {
  background: #CCCCCC;
  border-color: var(--color-border);
}
.md-card-item.selected {
  border: 4rpx solid var(--color-warning);
  box-shadow: 0 4rpx 16rpx rgba(255,149,0,0.35);
  transform: scale(1.12);
  position: relative;
  z-index: 2;
}
.md-card-num-text {
  font-size: var(--font-lg);
  font-weight: 800;
  color: #FFFFFF;
}

/* ========== 线下收银（嵌入） ========== */
.mof-amount-area {
  background: linear-gradient(135deg, var(--color-brand-dark-red), var(--color-brand-main-red));
  padding: 48rpx 32rpx;
  text-align: center;
}
.mof-currency {
  font-size: var(--font-xl);
  color: rgba(255,255,255,0.6);
  margin-right: 8rpx;
}
.mof-amount {
  font-size: 80rpx;
  font-weight: 700;
  color: #fff;
}

.mof-section {
  padding: 24rpx;
}
.mof-section-title {
  font-size: var(--font-base);
  color: var(--color-text-3);
  display: block;
  margin-bottom: 16rpx;
}

.mof-pay-types {
  display: flex;
  gap: 16rpx;
}
.mof-pay-type {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  border-radius: var(--radius-md);
  font-size: var(--font-base);
  background: var(--color-bg-card);
  color: var(--color-text-3);
  border: 2rpx solid var(--color-border);
}
.mof-pay-type.on {
  background: var(--color-primary-pale);
  color: var(--color-primary);
  border-color: var(--color-primary);
}

.mof-no-card {
  font-size: var(--font-md);
  color: var(--color-danger);
}

.mof-submit-wrap {
  padding: 0 24rpx 16rpx;
}
.mof-submit {
  width: 100%;
  background: linear-gradient(to right, var(--color-brand-main-red), var(--color-brand-chili-red));
  color: #fff;
  border-radius: var(--radius-full);
  font-size: var(--font-base);
  font-weight: 600;
  padding: 24rpx 0;
  border: none;
  box-shadow: 0 4rpx 12rpx var(--color-brand-soy-red-shadow);
}
.mof-submit[disabled] {
  background: #C7C7CC;
}

/* ========== 计算器弹窗 ========== */
.calc-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.5);
  z-index: 1100;
  display: flex;
  align-items: flex-end;
  justify-content: center;
}
.calc-modal {
  width: 100%;
  background: var(--color-bg-page);
  border-radius: 24rpx 24rpx 0 0;
  padding: 32rpx 24rpx;
  padding-bottom: calc(32rpx + 100rpx + env(safe-area-inset-bottom));
  animation: calcSlideUp 0.25s ease;
}
@keyframes calcSlideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}
.calc-screen {
  background: var(--color-bg-card);
  border-radius: var(--radius-md);
  padding: 24rpx;
  min-height: 120rpx;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  justify-content: flex-end;
  margin-bottom: 16rpx;
}
.calc-expr {
  font-size: var(--font-md);
  color: var(--color-text-3);
  word-break: break-all;
  text-align: right;
  min-height: 32rpx;
}
.calc-display-row {
  display: flex;
  align-items: baseline;
  gap: 4rpx;
}
.calc-currency {
  font-size: var(--font-xl);
  color: var(--color-text-3);
  font-weight: 600;
}
.calc-display {
  font-size: 60rpx;
  font-weight: 700;
  color: var(--color-text-1);
}
.calc-keyboard {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}
.calc-row {
  display: flex;
  gap: 10rpx;
}
.calc-key {
  flex: 1;
  background: var(--color-bg-card);
  border-radius: var(--radius-md);
  text-align: center;
  padding: 24rpx 0;
  font-size: var(--font-xl);
  font-weight: 500;
  color: var(--color-text-1);
}
.calc-key:active {
  background: var(--color-bg-page);
}
.calc-key-func {
  background: var(--color-bg-page);
  color: var(--color-text-2);
  font-size: var(--font-base);
}
.calc-key-op {
  background: var(--color-primary-pale);
  color: var(--color-primary);
  font-weight: 600;
}
.calc-key-eq {
  background: linear-gradient(135deg, var(--color-brand-gold), var(--color-brand-yolk));
  color: #fff;
  font-weight: 700;
}
.calc-key-confirm {
  background: linear-gradient(to right, var(--color-brand-main-red), var(--color-brand-chili-red));
  color: #fff;
  font-size: var(--font-md);
  font-weight: 700;
  box-shadow: 0 4rpx 12rpx var(--color-brand-soy-red-shadow);
}

/* ========== 金额点击提示 ========== */
.mof-amount-hint {
  font-size: var(--font-sm);
  color: rgba(255,255,255,0.5);
  display: block;
  margin-top: 4rpx;
}

.mof-result {
  text-align: center;
  padding: 80rpx 32rpx;
}
.mof-check {
  font-size: 104rpx;
  color: var(--color-primary);
  display: block;
}
.mof-result-title {
  font-size: 44rpx;
  font-weight: 700;
  color: var(--color-text-1);
  display: block;
  margin: 24rpx 0 40rpx;
}
.mof-result-card {
  background: var(--color-bg-card);
  border-radius: var(--radius-lg);
  padding: 32rpx;
  text-align: left;
  margin-bottom: 40rpx;
}
.mof-result-row {
  display: flex;
  justify-content: space-between;
  padding: 16rpx 0;
  border-bottom: 1rpx solid var(--color-border);
}
.mof-result-l {
  color: var(--color-text-3);
  font-size: var(--font-base);
}
.mof-result-v {
  color: var(--color-text-1);
  font-size: var(--font-base);
  font-weight: 500;
}
.mof-result-num {
  font-size: 44rpx;
  font-weight: 800;
  color: var(--color-primary);
}
.mof-result-price {
  font-size: var(--font-lg);
  font-weight: 700;
  color: var(--color-primary);
}
.mof-result-btns {
  display: flex;
  gap: 20rpx;
}
.mof-result-btn {
  flex: 1;
  background: linear-gradient(to right, var(--color-brand-main-red), var(--color-brand-chili-red));
  color: #fff;
  border-radius: var(--radius-full);
  font-size: var(--font-base);
  padding: 24rpx 0;
  border: none;
  box-shadow: 0 4rpx 12rpx var(--color-brand-soy-red-shadow);
}
.mof-result-btn-back {
  background: var(--color-brand-dark-red);
}
